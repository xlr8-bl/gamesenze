"""GameSenze — zero-cost sports analysis pipeline.

Design rule that runs through every module here: fail closed. An unpublished
pick costs nothing; a published pick built on bad data costs the track record.
"""

__version__ = "0.1.0"
